import XCTest
@testable import SwiftyUserInterface

final class SwiftyUserInterfaceTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftyUserInterface().text, "Hello, World!")
    }
}
